p = malloc(10000);
if (p == NULL) {
	/* allocation failed; take appropriate action */
}

// Alternation
if ((p = malloc(10000)) == NULL) {
	
}

void *malloc(size_t size);

char *p;
p = malloc(n+1);

/* Initiation */
strcpy(p, "abc");
